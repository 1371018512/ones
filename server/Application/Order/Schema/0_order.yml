order_check:
    #subject:
        #limit:20
    type:
        limit: 100
    bill_no:
        limit: 20
    status:
        type: integer
        limit: 1
    created:
        type: datetime
    estimated_time:
        type: datetime
    total_weight:
        type: decimal
        precision: 14
        scale: 4
    remark:
        limit:50
    #accounting_method:
        #limit: 20
        #blank: true
    #ship_status:
        #limit: 20
    $meta:
        indexes:
        foreign:
            customer:
                restrict:
                    delete: CASCADE
            ship:
                restrict:
                    delete: CASCADE
            company:
                restrict:
                    delete: CASCADE
            workflow:
                options:
                    blank: true
                restrict:
                    delete: SET_NULL
        enable_trash: true

order_check_detail:
    quantity:
        type: decimal
        precision: 14
        scale: 4
    product_unique_id:
        type: string
        limit: 100
    unit_price:
        type: decimal
        precision: 14
        scale: 4
    subtotal_amount:
        type: decimal
        precision: 14
        scale: 4
    already_deliver:
        type: decimal
        precision: 14
        scale: 4
    remark:
        blank: true
    $meta:
        indexes:
            product_unique_id:
        foreign:
            product:
                restrict:
                    delete: CASCADE
            order:
                restrict:
                    delete: CASCADE
            company:
                restrict:
                    delete: CASCADE
        enable_trash: true

order_dispatch:
    #subject:
        #limit:20
    type:
        limit: 100
    bill_no:
        limit: 20
    status:
        type: integer
        limit: 1
    dispatch_time:
        type: datetime
    delivery_place:
        limit: 100
    total_weight:
        type: decimal
        precision: 14
        scale: 4
    remark:
        limit:50
    $meta:
        indexes:
        foreign:
            order:
            #发货单所属的订单号
                restrict:
                    delete: CASCADE
            customer:
                restrict:
                    delete: CASCADE
            ship:
                restrict:
                    delete: CASCADE
            company:
                restrict:
                    delete: CASCADE
            workflow:
                options:
                    blank: true
                restrict:
                    delete: SET_NULL
        enable_trash: true

order_dispatch_detail:
    quantity:
        type: decimal
        precision: 14
        scale: 4
    product_unique_id:
        type: string
        limit: 100
    real_quantity:
        type: decimal
        precision: 14
        scale: 4
    remark:
        blank: true
    $meta:
        indexes:
            product_unique_id:
        foreign:
            product:
                restrict:
                    delete: CASCADE
            order:
                #发货单号
                restrict:
                    delete: CASCADE
            company:
                restrict:
                    delete: CASCADE
        enable_trash: true